{
    "name":         "zstate/crawler",
    "description":  "Crawler",

    "require": {
        "php": ">=5.6.0",
        "guzzlehttp/guzzle": "^6.0",
        "symfony/console": "^3.0",
        "symfony/dom-crawler": "^3.1",
        "psr/log": "^1.0"
    },

    "require-dev": {
        "phpunit/phpunit": "^5.0"
    },

    "autoload": {
        "psr-4": {"Zstate\\Crawler\\": "src/"}
    },

    "config": {
        "bin-dir": "bin"
    }
}
